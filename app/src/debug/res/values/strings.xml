<?xml version="1.0" encoding="utf-8"?>
<!--
    LOCAL ONLY (develop branch). Do not include in upstream PRs.
    Overrides the launcher label so the debug build is distinguishable
    from the store release when both are installed.
-->
<resources>
    <string name="app_name" translatable="false">Sudoku DEV</string>
</resources>
